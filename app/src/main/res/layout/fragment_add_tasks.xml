<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <import type="android.view.View"/>
        <variable
            name="viewModel"
            type="com.example.cryptofunding.viewmodel.HandleTasksViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".AddTasksFragment"
        android:background="@color/colorPrimaryApp">

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/topBarGuideline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintGuide_begin="?attr/actionBarSize"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/tasksRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="@id/topBarGuideline"
            app:layout_constraintBottom_toBottomOf="parent"
            android:paddingBottom="68dp"
            android:clipToPadding="false"
            tools:listitem="@layout/item_task"/>

        <Button
            android:id="@+id/buttonLaunchProject"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="13dp"
            android:layout_marginEnd="13dp"
            android:layout_marginStart="13dp"
            android:textSize="21sp"
            android:textColor="@color/colorBackgroundWhiteApp"
            android:fontFamily="@font/opensans_semibold"
            android:background="@drawable/selector_button_state"
            android:elevation="6dp"
            android:text="@string/launch_project"
            android:enabled="@{viewModel.canProceed}"/>

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/loadingAnimation"
            android:layout_width="110dp"
            android:layout_height="110dp"
            app:layout_constraintTop_toTopOf="@id/buttonLaunchProject"
            app:layout_constraintBottom_toBottomOf="@id/buttonLaunchProject"
            app:layout_constraintStart_toStartOf="@id/buttonLaunchProject"
            app:layout_constraintEnd_toEndOf="@id/buttonLaunchProject"
            android:elevation="8dp"
            app:lottie_fileName="loadingn.json"
            app:lottie_loop="true"
            app:lottie_autoPlay="true"
            android:visibility="gone"/>

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/successAnimation"
            android:layout_width="50dp"
            android:layout_height="50dp"
            app:layout_constraintTop_toTopOf="@id/buttonLaunchProject"
            app:layout_constraintBottom_toBottomOf="@id/buttonLaunchProject"
            app:layout_constraintStart_toStartOf="@id/buttonLaunchProject"
            app:layout_constraintEnd_toEndOf="@id/buttonLaunchProject"
            android:elevation="8dp"
            app:lottie_fileName="success.json"
            app:lottie_loop="false"
            app:lottie_autoPlay="true"
            android:visibility="gone"/>

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/failureAnimation"
            android:layout_width="65dp"
            android:layout_height="65dp"
            app:lottie_fileName="failure.json"
            app:layout_constraintTop_toTopOf="@id/buttonLaunchProject"
            app:layout_constraintBottom_toBottomOf="@id/buttonLaunchProject"
            app:layout_constraintStart_toStartOf="@id/buttonLaunchProject"
            app:layout_constraintEnd_toEndOf="@id/buttonLaunchProject"
            android:elevation="8dp"
            app:lottie_loop="false"
            app:lottie_autoPlay="true"
            android:visibility="gone"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>